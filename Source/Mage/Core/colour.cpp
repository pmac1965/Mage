#include "colour.h"


namespace Core
{
    const Colour    Colour::White(1.0f, 1.0f, 1.0f);
    const Colour    Colour::Black(0.0f, 0.0f, 0.0f);
    const Colour    Colour::Red(1.0f, 0.0f, 0.0f);
    const Colour    Colour::Green(0.0f, 1.0f, 0.0f);
    const Colour    Colour::Blue(0.0f, 0.0f, 1.0f);
    const Colour    Colour::Yellow(1.0f, 1.0f, 0.0f);
    const Colour    Colour::Cyan(0.0f, 1.0f, 1.0f);
    const Colour    Colour::Magenta(1.0f, 0.0f, 1.0f);
    const Colour    Colour::DarkGray(0.250f, 0.250f, 0.250f);
    const Colour    Colour::Gray(0.500f, 0.500f, 0.500f);
    const Colour    Colour::LiteGray(0.750f, 0.750f, 0.750f);
    const Colour    Colour::Brown(0.60f, 0.40f, 0.12f);
    const Colour    Colour::Orange(0.98f, 0.625f, 0.12f);
    const Colour    Colour::PastelPink(0.98f, 0.04f, 0.7f);
    const Colour    Colour::BarneyPurple(0.60f, 0.40f, 0.70f);
}
